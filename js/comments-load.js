/* import { bigPicture } from './full-image.js';
let commentsLoader = bigPicture.querySelector('.comments-loader')
let commentsCount = bigPicture.querySelector('.social__comment-count')


const allComments = document.querySelectorAll('.social__comment')

const SHOW_COMMENTS_COUNT = 5;

const getCommentsCount = (comment) => {
  for (let i = SHOW_COMMENTS_COUNT; i < comment.length; i++) {
    comment[i].classList.add('hidden');
  }
}

getCommentsCount()
 */
